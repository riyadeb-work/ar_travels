/**
 * @file
 * AR Travels UI behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.artravelsUi = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
